1. 生成的计划页面美化不够，可以生成一个置中的小卡片，一条一条列举。见图片1
2. 生成的计划没办法直接编辑，见图片2
3. 只执行复杂任务的时候使用大量的工具导致回话都让页面占据，也是以计划为一个纬度统一放到一个下拉框里，点击计划查看对应的下拉框
4. 计划执行的时候不能动态加载，让我在页面上不能看出来到底现在走到哪一步了
5. 审批通过的时候太僵硬，可以在计划卡片的下面放一个执行构建的按钮
6. 执行的时候没办法停止，放置一个按钮用来让我停止和构建按钮安置到一起，在点击停止的时候必须是当前步骤执行完再停止
7. 在生成几条可以在页面用户输入，用来测试plan问题的，用户问题





8. 生成的plan是能修改，不能增加新的步骤
9. 现在还是会把每个工具使用都会单独列出了，导致回话框都是使用的工具，修改成将使用的工具进行分步骤展示，每一个步骤下面展示对应的使用工具，执行到哪一步展开哪一步的工具，步骤执行完自动将该步骤的工具折叠
10. 任务执行计划出现问题，见图片
11. 创建查看历史任务功能，可以在页面上查看历史任务的执行步骤和产出结果，前端页面一个单独的标签展示，以列表形式展示，携带根据agent筛选功能，和删除任务功能，

12.在当前agent回话页面的右侧，可以查看当前回话历史当前回话的近5个任务，再右边最下面展示< 1/5 >来进行切换，右边框有点拥挤，可以增加一个滚动条，再给右侧增加一个收缩的>这个按钮，可以控制是否看这个计划执行，

存在问题
1. 当右侧面板，布局拥挤，增加滚动
2. 重要：回话框展示，限制执行的步骤下拉框展示执行的工具，到第二步时，收起上一步的工具展示，展示第二步展示执行的工具，回话排版，执行步骤下拉框，展示需要执行的步骤，->该步骤执行的输出，循环接下来的步骤
3. 当前 / 共 2 个计划，展示有问题， 第几个/共几个
4. 点击停止按钮功能未生效：执行的时候没办法停止，放置一个按钮用来让我停止和构建按钮安置到一起，在点击停止的时候必须是当前步骤执行完再停止
5. 执行完成之后没有任务历史的记录
6. 没办法手动拖动右侧面板任务步骤的顺序






1. 在plan执行的过程中可以不用模型的输出，见图片/Users/user/Documents/code/GitHub/JavaClaw/docs/process/plan/image.png，不用输出 【好的，我将开始执行计划。首先标记第0步为进行中状态。现在开始执行步骤0：创建项目目录结构。我将创建后端、前端、数据库和 Docker 配置所需的目录。】这句句话，有执行的过程就可以，在plan执行完成之后输出过程和总结就可以了