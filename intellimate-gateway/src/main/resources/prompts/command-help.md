可用指令：

  /clear            — 清除当前对话（自动持久化记忆后清除历史）
  /status           — 查看当前会话状态（模型、工具、连接信息）
  /model <名称>     — 切换 LLM 模型（如 /model qwen-plus）
  /plan <描述>      — 强制以 Plan 模式处理任务（如 /plan 重构认证模块）
  /approve <code>   — 审批 DM 配对请求
  /help             — 显示此帮助
